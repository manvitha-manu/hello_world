import React from 'react';

function ChildComponent(props) {
    return (
        <div>
            <button>This is the ParentComponent</button>
        </div>
    );
}

export default ChildComponent;